# Next Newsletter — Queue

Fill in the fields below before the Thursday ~11:00 AM IST newsletter routine runs.
The routine reads this file, sends the announcement using whatever is filled in here,
then clears the fields back to blank (see `newsletter_routine/README.md` for the full
mechanism). If this file is still blank when the routine runs, it sends nothing and
just reports "no article queued this week" — it never resends an old article.

---

Title:
Link:
Secondary hook (optional, one sentence used in the intro — leave blank to use the default "Quick note, not a pitch this time." opener):

---
Last sent: (the routine fills this in automatically after a successful send — do not edit)
